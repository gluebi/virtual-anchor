node_modules/
dist/
coverage/
docs-api/
.turbo/
*.tsbuildinfo

# Benchmark records. Kept out of the repo because a frame time is a fact about the machine that
# produced it, and a committed one would invite comparison across machines that never met.
perf/results/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

.DS_Store
*.log
.vite/

# Copied into the package at release time by scripts/prepare-package.mjs.
packages/virtual-anchor/README.md
packages/virtual-anchor/LICENSE
